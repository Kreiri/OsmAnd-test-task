package com.example.osmandtesttask.common


enum class MapDownloadQualifier {
    HTTP_CLIENT, BASE_URL, RETROFIT, FOLDER
}

enum class ScopeQualifier {
    APP_SCOPE
}

enum class ProviderQualifiers {
    LOCALE, ASSET
}